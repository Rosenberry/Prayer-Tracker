<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.learning.matthew.myapplication.AddNewPrayer">

    <EditText
        android:id="@+id/editName"
        android:textSize="40sp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Name"/>

    <EditText
        android:textSize="30sp"
        android:id="@+id/editMessage"
        android:layout_below="@id/editName"
        android:layout_width="match_parent"
        android:layout_height="150dp"
        android:hint="Message"/>

    <Spinner
        android:id="@+id/add_spinner"
        android:layout_width="wrap_content"
        android:layout_height="75dp"
        android:layout_below="@id/editMessage"></Spinner>

    <Button
        android:id="@+id/createPrayerButton"
        android:onClick="sendInformation"
        android:layout_width="match_parent"
        android:layout_height="75sp"
        android:layout_below="@id/add_spinner"
        android:text="Click here to create a new prayer"/>

</RelativeLayout>
